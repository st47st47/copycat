import React from 'react'
import ReactDOM from 'react-dom/client'

import Routing from '../brg/Routing.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Routing />
  </React.StrictMode>,
)
